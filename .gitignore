# ── Secrets / local config (never commit) ──────────────────────────────
# EmailJS credentials live in config.js (loaded by the browser at runtime)
config.js
.env
.env.local
.env.*.local

# ── OS / editor cruft ──────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# ── Dependencies / build (in case tooling is added later) ───────────────
node_modules/
dist/
.vercel/

.vercel
.env*
